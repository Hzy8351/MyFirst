public enum UIEnum
{
    //大厅UI
    HomeUI = 1,

    // 游戏
    JoyStickUI = 1000,
    BattleHeroUI = 1001,

    // 大厅的二级UI(2000及以上的UI只要有一个还开着，场景就不能拖动)
    SettingUI = 2000,

    // GM工具结束
    GMUI = 10000,
}